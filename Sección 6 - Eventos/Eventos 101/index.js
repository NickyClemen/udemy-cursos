/* Eventos -> Acciones que ejecutan una función.
El 'on' de los eventos, hay que sacarlos si no se etsán usando dentro del HTML.
    [variable].addEventListener(event, función); 
    
En un evento click, el isTrusted determina si el evento lo ejecutó un usuario. */