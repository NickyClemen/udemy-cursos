/* Instalar CSS loader:
    npm install style-loader css-loader --save-dev */
import '../css/style.css';

const producto = ['Producto 1', 'Producto 2', 'Producto 3'];
producto.push('Producto 4');
console.log(producto);
