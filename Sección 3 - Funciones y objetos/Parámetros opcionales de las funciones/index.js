/* El Javascript, por defecto, todos los parámetros son opcionales. */
function nombreCompleto(nombre, apellido) {
    return nombre + " " + apellido + ".";
}
var nombre = nombreCompleto('Wally', 'West');
console.log(nombre);
