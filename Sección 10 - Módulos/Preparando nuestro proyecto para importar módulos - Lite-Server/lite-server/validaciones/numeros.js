"use strict";
exports.__esModule = true;
var PI = 3.1416;
function restar(numUno, numDos) {
    return (numUno - numDos);
} // Se usa la palabra default para que sea el módulo por defecto (se importa eso solo).
exports["default"] = restar;
