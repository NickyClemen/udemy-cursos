/* El tsconfig.json tiene diferentes importadores de módulos.
Systemjs es un importador de módulos (Universal Dynamic Module Loader). */
import { PI } from './validaciones/numeros';

console.log(PI);
