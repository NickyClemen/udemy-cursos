package apuntes;

public class ValorReferencia {

	public static void main(String[] args) {
		
		/* Paso por valor
		Usar tipos primitivos para enviar valores a los métodos. Se envía una copia
		del valor de la variable. Para modificiar el variable, hay que hacerlo dentro
		del scope en la que se encuentra.
		
		Paso por referencia
		Se utiliza cuando se envíen objetos por parámetro. Se envía la referencia del objeto,
		por lo que no se envía una copia, por lo que se la modifica directamente. */

	}

}
