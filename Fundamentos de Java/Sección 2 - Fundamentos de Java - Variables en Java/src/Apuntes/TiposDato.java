package Apuntes;

public class TiposDato {

	public static void main(String[] args) {
		
		/* Tipos de datos en Java.
     	1) Primitivos (almacenan un solo valor).
     		a) Integer.
     		b) Float.
     		c) Boolean.
     	2) Tipos referenciados (Object).
     		a) Class.
     		b) Interface.
     		c) Array. 
     
     		
     Tipos primitivos en Java.
     		1) Enteros.
     			a) byte: Ocupa 8 bits. Valor por default: 0.
     			b) short: Ocupa 16 bits. Valor por default: 0.
     			c) char: Ocupa 16 bits, pero maneja Unicode para almanecar valores de tipo char. Valor por default: \u0000 (null).
     			d) int: Ocupa 32 bits. Valor por default: 0.
     			e) long: Ocupa 64 bits. Valor por default: 0.
     			f) boolean: Puede almacenar true o false. Su valor por default es false.
     		
     		2) Flotantes.
     			a) Float: Ocupa 32 bits. Valor por default: 0.0.
     			b) double: Ocupa 64 bits. Valor por default: 0.0. */
		
	}

}
