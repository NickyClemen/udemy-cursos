package Apuntes;

public class Concatenacion {

	public static void main(String[] args) {
		
		/* Al '+' se lo conoce como 'operador sobrecargado', ya que concatena y suma. 
		Las expresiones se evalúan de izquierda a derecha.
		Si se suma un string con enteros, al estar primero (por contexto String), los concatena, no los suma. 
		Si primero hay enteros, primero los suma, y después concatena strings.
		Tener cuidado con el orden de las variable. */

	}

}
