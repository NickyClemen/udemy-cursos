package apuntes;

public class Memoria {

	public static void main(String[] args) {
		
		/* Memoeria Stack y Heap
		Stack: Se utilizan para almacenar las variables locales, y las llamadas de funciones.
		Las variables almacenadas en ésta memoria, tienen un periodo de vida corto (existen mientras
		se ejecuta el método o la función).s 
		Heap: Se utiliza para almacenar objetos y sus atributos. La vida del objeto es más prolongada.
		No se almanena el objeto en sí mismo, sino una referencia al objeto. 
		La referencia (dirección de memoria) es representada por un hexagecimal.
		
		Diferencia importante entre clase y objeto: La clase define un tipo, para ser posteriormente
		instanciado (con el objeto de crear objetos). Son los objetos los que ocupan un espacio real
		en memoeria. 
		
		Asginando referencias de memoria
			
			Perosna p1 = new Persona();
			Persona p2 = p1 (0x333). */

	}

}
