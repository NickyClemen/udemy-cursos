let helloWord = 'Hola mundo';
/* Es outFile es un archivo que junta todo lo que le especifiquemos en un único archivo.
Typescript genera un archivo de salida (no compilado), que combina todos los archivos. 
    tsc --outFile main.js 
    nombre del outFile + archivos que se quieren unir.
Para evitar estar reemplazando el nombre del script en el HTML, especificando el outFile en
el tsconfig.json. */

