/* lenght: Retorna el largo del string.
concat(): Concatena strings. Acepta dos parámetros.
toUpperCase(): Convierte en mayúsculas.
toLowerCase(): Convierte en minúsculas.
indexOf: Busca la primera coincidencia. Retorna la posición. Si no encuentra nada, retorna -1.
substring(): Corta el texto, y retorna otro string.
slice(): Corta el string. Hay que indicarle por parámetro desde qué posició, y cuántos caracteres 
hay que cortar.
split(): Corta el string y lo retorna en un artay. El parámetro es la lógica de corte.
replace(): Reeemplaza strings.
includes(): Como indexOf(), pero retorna boolean.
repeat(): Repite el string. */